# 🛠️ Localhost Development Overrides for Vite Dev Server (npm run dev)
# Accessed via http://localhost:5173/

# Point to the Vite proxy defined in vite.config.js
VITE_API_URL=/server

# Router base should be root when running on Vite dev server
VITE_ROUTER_BASE=/
